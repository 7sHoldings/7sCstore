# Copy to .env and adjust. For production switch DATABASE_URL to a Postgres URL
# and update the datasource provider in prisma/schema.prisma to "postgresql".
DATABASE_URL="file:./dev.db"

# Secret used to sign session JWTs. Generate a long random string for production.
AUTH_SECRET="change-me-to-a-long-random-string-in-production"
